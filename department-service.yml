spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_SERVER_URL:localhost:3306/department_db/}
    username: newuser
    password: newuser
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  
